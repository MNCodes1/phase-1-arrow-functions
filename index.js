// Function expression called divide
const divide = function() {
  return 2000 / 100;
};

// Arrow function called square — takes one parameter and multiplies it by itself
const square = (num) => num * num;

// Arrow function called add — takes two parameters and adds them together
const add = (a, b) => a + b;